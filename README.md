# video_editor
 基于Python tkinter图形界面+FFmpeg/FFprobe后端的视频编辑工具，无需安装额外 Python 第三方库（仅使用标准库）。/A video editing tool with a Python Tkinter graphical interface and FFmpeg/FFprobe backend, requiring no additional Python third-party libraries (using only the standard library).
